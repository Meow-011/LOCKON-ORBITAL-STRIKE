python -m venv venv (just one time)
.\venv\Scripts\activate
pip install -r requirements.txt

(if not work use <pip install flet packaging requests pillow aiohttp beautifulsoup4 playwright pyjwt>)

(venv) python main.py